{{#if data.system}}
    <section class="estoque-table" style="margin-top: -100vh;">
{{else}}
    <section class="estoque-table">
{{/if}}
    <div class="container">
        {{> table }}
    </div>
</section>