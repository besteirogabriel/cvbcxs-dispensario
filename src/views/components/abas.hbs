<!-- abas de navegação login e cadastro de loja -->
<div class="abas d-flex">
    {{#each abas}}
    <a href="{{this.url}}" class="aba {{this.class}}">
        {{{this.icon}}}
        {{this.name}}
    </a>
    {{/each}}
</div>